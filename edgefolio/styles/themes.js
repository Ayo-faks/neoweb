const lime = require('./theme.lime')
const amber = require('./theme.amber')
const rose = require('./theme.rose')
const cyan = require('./theme.cyan')
const custom = require('./theme.custom')

module.exports = {
  default: custom,
  lime,
  amber,
  rose,
  cyan,
}
