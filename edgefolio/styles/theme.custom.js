const colors = require('tailwindcss/colors')

const theme = {
  // Primary color
  alpha: colors.rose,
  // Secondary color
  beta: colors.amber,
  // Accent color
  accent: colors.blue,
  // Neutral color
  omega: colors.slate
}

module.exports = theme